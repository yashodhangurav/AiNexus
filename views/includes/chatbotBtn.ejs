<div class="chatbot-container">
  
  <div class="chat-welcome-tooltip shadow-lg">
    Hi there! Need help finding an AI? ✨
  </div>

  <a href="/home/chatbot" class="nexus-chat-btn">
    
    <div class="indicator-pulse"></div>
    <div class="chat-indicator"></div>

    <div class="ripple-ring"></div>
    
    <div class="btn-content">
      <i class="fa-solid fa-robot bot-icon"></i>
      <span class="bot-text">Ask AiNexus</span>
    </div>
  </a>

</div>

<style>
  /* Container positioning */
  .chatbot-container {
    position: fixed;
    bottom: 30px;
    right: 30px;
    z-index: 1050;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    font-family: 'Plus Jakarta Sans', sans-serif;
  }

  /* --- Welcome Tooltip Bubble --- */
  .chat-welcome-tooltip {
    background: linear-gradient(135deg, #1e1b4b, #312e81);
    color: #e4e4e7;
    padding: 10px 18px;
    border-radius: 16px 16px 0px 16px; /* Chat bubble tail effect */
    font-size: 0.9rem;
    font-weight: 500;
    margin-bottom: 15px;
    border: 1px solid rgba(168, 85, 247, 0.4);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5);
    opacity: 1;
    transform: translateY(0);
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    animation: floatTooltip 4s ease-in-out infinite;
  }

  /* Hide the tooltip when the user hovers over the button */
  .chatbot-container:hover .chat-welcome-tooltip {
    opacity: 0;
    transform: translateY(10px) scale(0.9);
    pointer-events: none;
  }

  /* --- Expandable Pill Button --- */
  .nexus-chat-btn {
    position: relative;
    background: rgba(15, 15, 15, 0.85);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(168, 85, 247, 0.4);
    height: 65px;
    width: 65px; /* Starts as a circle */
    border-radius: 50px;
    text-decoration: none;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    box-shadow: 0 10px 30px rgba(99, 102, 241, 0.2);
    display: flex;
    align-items: center;
  }

  /* Expand on Hover */
  .nexus-chat-btn:hover {
    width: 190px; /* Expands into a pill */
    border-color: #c084fc;
    background: linear-gradient(135deg, rgba(49, 46, 129, 0.9), rgba(88, 28, 135, 0.9));
    box-shadow: 0 10px 40px rgba(168, 85, 247, 0.4);
  }

  /* Content Wrapper inside Button */
  .btn-content {
    display: flex;
    align-items: center;
    padding-left: 20px;
    width: 100%;
  }

  .bot-icon {
    font-size: 1.6rem;
    color: #c084fc;
    flex-shrink: 0;
    transition: all 0.4s ease;
    z-index: 2;
  }

  .bot-text {
    color: #ffffff;
    font-weight: 600;
    font-size: 1.05rem;
    margin-left: 12px;
    white-space: nowrap;
    opacity: 0;
    transform: translateX(10px);
    transition: all 0.4s ease;
    z-index: 2;
  }

  /* Animate inner content on hover */
  .nexus-chat-btn:hover .bot-icon {
    color: #ffffff;
    transform: rotate(-15deg) scale(1.1);
  }

  .nexus-chat-btn:hover .bot-text {
    opacity: 1;
    transform: translateX(0);
  }

  /* --- Unread Notification Badge --- */
  .chat-indicator {
    position: absolute;
    top: 2px;
    right: 2px;
    width: 14px;
    height: 14px;
    background-color: #ef4444; /* Alert Red */
    border: 2px solid #050505;
    border-radius: 50%;
    z-index: 10;
    transition: opacity 0.3s ease;
  }

  .indicator-pulse {
    position: absolute;
    top: 2px;
    right: 2px;
    width: 14px;
    height: 14px;
    background-color: #ef4444;
    border-radius: 50%;
    z-index: 9;
    animation: pingRed 2s cubic-bezier(0, 0, 0.2, 1) infinite;
    transition: opacity 0.3s ease;
  }

  /* Hide badge when expanded */
  .nexus-chat-btn:hover .chat-indicator,
  .nexus-chat-btn:hover .indicator-pulse {
    opacity: 0;
  }

  /* --- Ambient Background Ripple --- */
  .ripple-ring {
    position: absolute;
    top: 50%;
    left: 32.5px; /* Center of the initial 65px circle */
    width: 0px;
    height: 0px;
    background: rgba(168, 85, 247, 0.2);
    border-radius: 50%;
    transform: translate(-50%, -50%);
    animation: ringExpand 3s infinite;
    z-index: 1;
    pointer-events: none;
  }

  .nexus-chat-btn:hover .ripple-ring {
    display: none; /* Stops ripple when actively interacting */
  }

  /* --- Keyframe Animations --- */
  @keyframes floatTooltip {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-6px); }
  }

  @keyframes pingRed {
    75%, 100% { transform: scale(2.5); opacity: 0; }
  }

  @keyframes ringExpand {
    0% { width: 0; height: 0; opacity: 1; }
    100% { width: 100px; height: 100px; opacity: 0; }
  }

  /* Mobile Adjustments */
  @media (max-width: 768px) {
    .chat-welcome-tooltip {
      display: none; /* Hide tooltip on mobile to save screen space */
    }
    
    .chatbot-container {
      bottom: 20px;
      right: 20px;
    }
  }
</style>